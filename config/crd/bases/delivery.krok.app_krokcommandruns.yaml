---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: krokcommandruns.delivery.krok.app
spec:
  group: delivery.krok.app
  names:
    kind: KrokCommandRun
    listKind: KrokCommandRunList
    plural: krokcommandruns
    singular: krokcommandrun
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KrokCommandRun is the Schema for the krokcommandruns API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KrokCommandRunSpec defines the desired state of KrokCommandRun
            properties:
              commandName:
                description: CommandName is the name of the command that is being
                  executed.
                type: string
              eventRef:
                description: EventRef is the ref of the event that this run belongs
                  to.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - commandName
            - eventRef
            type: object
          status:
            description: KrokCommandRunStatus defines the observed state of KrokCommandRun
            properties:
              outcome:
                description: Outcome is any output of the command. Stdout and stderr
                  combined.
                type: string
              status:
                description: 'Status is the current state of the command run. example:
                  created, running, failed, success'
                type: string
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
